# No environment variables are required for the basic setup.
# Hiro API is public (no API key needed for standard rate limits).
# For higher rate limits, you can add an API key:
# HIRO_API_KEY=your_api_key_here
# Then update src/lib/hiro-api.ts to include: "x-api-key": process.env.HIRO_API_KEY
